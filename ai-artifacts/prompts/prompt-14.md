Modify ONLY this existing file:

`tests/ui/pages/autocomplete-form.page.js`

Do not create, modify, or delete any other file.

Change ONLY the implementation of:

`getVisibleSuggestionTexts()`

The method is intended to return the text of suggestion `<li>` elements that are currently visible.

Update it so that it retrieves only visible suggestion items, while preserving the existing Page Object structure and selector strategy.

Use the existing:

`this.suggestionItems`

Do not change any selectors, URLs, other methods, class names, or test files.

Do not add assertions.

Do not add waits or arbitrary timeouts.

After making this single correction, stop.