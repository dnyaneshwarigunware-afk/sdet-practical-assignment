Do NOT create, modify, or delete any files.

Run the existing Playwright test suite only.

First run:

npm test

After execution, report:

1. Total tests discovered
2. Tests passed
3. Tests failed
4. Tests skipped/fixme
5. Exact failure messages
6. Whether the failures are caused by:
   - test code
   - Playwright configuration
   - missing dependency/browser
   - unavailable assignment URL/environment
   - an explicitly documented assignment ambiguity

Do NOT modify any test or configuration file to make tests pass.

Do NOT retry with invented URLs, API endpoints, credentials, or application behavior.

Do NOT create any files.

After reporting the results, STOP and wait for review.